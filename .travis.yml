language: java
before_script:
 - chmod +x gradlew
jobs:
  include:
  - stage: "Initial Assemble"
    language: java
    script:
    - ./gradlew assemble
  - stage: "Tests"
    language: java
    script:
    - ./gradlew test